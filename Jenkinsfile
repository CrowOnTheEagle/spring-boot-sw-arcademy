pipeline {
    agent any

    stages {
        // Check jenkins is connected to github
        // 깃허브와 연동 확인
        stage('0. 연결 확인') {
            steps {
                echo '스테이지 출발'
            }
        }
    }
}