기존 포트 사용중인 오류가 뜰 때
    - cmd에서 netstat -ano | find "LISTEN"
    - [::]:8081 혹은 0.0.0.0:8081을 찾고 pid를 복사
    - kill pid입력

cmd에서 리눅스 서버 원격접속
    - MobaXterm
    - cmd
        * ssh blizkrig553@localhost -p 22
        * 리눅스 서버 포트를 열어야 함
            # 설정 -> export -> 네트워크 -> 포트포워딩 들어가서 아래 정보 입력
                & 호스트 IP - 127.0.0.1 혹은 localhost
                & 호스트 포트 - 22
                & 게스트 IP - 자기 자신의 경우 생략
                & 게스트 포트 - 22

포트 포워딩
    - 특정 포트로 들어오면 연결시켜 주는것