기존 포트 사용중인 오류가 뜰 때
    - cmd에서 netstat -ano | find "LISTEN"
    - [::]:8081 혹은 0.0.0.0:8081을 찾고 pid를 복사
    - kill pid입력

cmd에서 리눅스 서버 원격접속
    - cmd
        * ssh blizkrig553@localhost -p 22
        * 리눅스 서버 포트를 열어야 함
            # 설정 -> export -> 네트워크 -> 포트포워딩 들어가서 아래 정보 입력
                & 호스트 IP - 127.0.0.1 혹은 localhost
                & 호스트 포트 - 22
                & 게스트 IP - 자기 자신의 경우 생략
                & 게스트 포트 - 22
    - Mobaxterm
        * https://mobaxterm.mobatek.net/download-home-edition.html 에서 다운
        * 실행
        * 세션 -> ssh
        * remote host에 localhost 입력, 포트번호 입력하고 확인
        * 이후 모두 yes 누름
    - putty

포트 포워딩
    - 특정 포트로 들어오면 연결시켜 주는것

메이븐 빌드 방법
    - https://maven.apache.org/download.cgi 링크에서 바이너리 zip 파일 다운
    - maven 확장 설정을 아래와 같이 적용
        * user settings의 경로를 %다운받은 메이븐 폴더%/conf/settings.xml 위치로 설정
        * excuteable path를 mvn.cmd 혹은 mvnw(.cmd) 파일 위치로 설정
    - 익스플로러 창에 maven 항목 띄우기
    - 프로젝트 우클릭 -> run maven commands -> install
    - jar 파일 위치: root/target/(프로젝트명).jar

리눅스 기본패캐지 설치
    - sudo apt-get update
    - sudo apt-get upgrade -y

가상머신 복제
    - 복제할 가상머신 오른쪽 클릭 -> 복제 -> 완전한 복제 -> 완료
    - 리눅스는 잘못되는경우가 많아 복제를 해둬야함

WSL
    - 윈도우 버전 10.0.19042부터 설치가능
    - wsl --install
    - wsl --set-default-version 2
    - wsl.exe -d Ubuntu 로 실행

annotation
    - @(어노테이션 이름)

도커 명령어
    - docker run
    - 없는 이미지는 다운받음
    - https://hub.docker.com/explore에서 이미지를 찾을 수 있음